name: Public Git Plugin Smoke

on:
  push:
    branches: [main]
    tags: ['v*.*.*']
  workflow_dispatch:

permissions:
  contents: read

jobs:
  smoke:
    uses: luluthehungrycat/omp-pi-sandbox-infra/.github/workflows/reusable-plugin-smoke.yml@main
    with:
      plugin_repository: luluthehungrycat/omp-pi-sandbox-oddsjam
